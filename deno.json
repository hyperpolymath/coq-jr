{
  "name": "@coq-jr/web",
  "version": "0.1.0",
  "tasks": {
    "build": "npx rescript build",
    "clean": "npx rescript clean",
    "dev": "npx rescript build && deno run --allow-net --allow-read src/Server.res.js",
    "serve": "deno run --allow-net --allow-read src/Server.res.js"
  },
  "compilerOptions": {
    "lib": ["deno.window"]
  }
}
