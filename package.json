{
  "private": true,
  "name": "coq-jr",
  "version": "0.1.0",
  "description": "jsCoq in ReScript - npm only needed for ReScript compiler",
  "scripts": {
    "build": "rescript build",
    "clean": "rescript clean"
  },
  "devDependencies": {
    "rescript": "^11.1.0"
  }
}
